// sqrt.cpp -- using sqrt() function

#include <iostream>
#include <cmath>

int main() {
	using namespace std;
	double area;
	cin >> area;
	double side;
	side = sqrt(area);
	cout << side;
	return 0;
}
